services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    working_dir: /backend
    ports:
      - "8000:8000"

  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    working_dir: /ui
    ports:
      - "3000:3000"